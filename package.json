{
  "name": "stashl",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter=@stashl/api dev & bun run --filter=@stashl/web dev",
    "dev:api": "bun run --filter=@stashl/api dev",
    "dev:web": "bun run --filter=@stashl/web dev",
    "dev:mobile": "bun run --filter=@stashl/mobile start",
    "dev:tasks": "bun run --filter=@stashl/tasks dev",
    "mobile:ios": "bun run --filter=@stashl/mobile ios",
    "mobile:android": "bun run --filter=@stashl/mobile android",
    "type-check": "bun run --filter=* type-check",
    "lint": "bun run --filter=* lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "bun x -y rimraf -g **/bun.lock* **/node_modules",
    "db:generate": "bun run --filter=@stashl/domain db:generate",
    "db:migrate": "bun run --filter=@stashl/domain db:migrate",
    "db:push": "bun run --filter=@stashl/domain db:push",
    "db:studio": "bun run --filter=@stashl/domain db:studio",
    "deploy:api": "fly deploy --config ./deploy/api/fly.toml --dockerfile ./deploy/api/Dockerfile",
    "deploy:web": "fly deploy --config ./deploy/web/fly.toml --dockerfile ./deploy/web/Dockerfile"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "@typescript-eslint/eslint-plugin": "^8.36.0",
    "@typescript-eslint/parser": "^8.37.0",
    "eslint": "^9.31.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "resolutions": {
    "rollup": "npm:@rollup/wasm-node"
  },
  "overrides": {
    "rollup": "npm:@rollup/wasm-node"
  }
}
